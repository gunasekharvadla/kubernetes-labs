apiVersion: v1
kind: Pod

metadata:
  name: nginx-dev
  namespace: devops-dev
  labels:
    app: nginx
    environment: development

spec:
  containers:
    - name: nginx
      image: nginx:1.27
      ports:
        - containerPort: 80
